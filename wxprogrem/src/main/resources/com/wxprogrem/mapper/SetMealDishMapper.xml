<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxprogrem.mapper.SetMealDishMapper">
    <insert id="insert">
        INSERT INTO setmeal_dish (name, setmeal_id, cover, price, acount)
        VALUES
        <foreach collection="setMealDishList" item="item" separator=",">
            (#{item.name}, #{item.setmealId}, #{item.cover}, #{item.price}, #{item.acount})
        </foreach>
    </insert>


</mapper>