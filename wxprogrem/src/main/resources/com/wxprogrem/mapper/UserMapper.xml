<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxprogrem.mapper.UserMapper">
    <select id="countDailyRegistrations" resultType="map">
        SELECT
            DATE(register_time) AS date,
            COUNT(*) AS count
        FROM
            user
        WHERE
            register_time BETWEEN #{startDate} AND #{endDate} + INTERVAL 1 DAY
        GROUP BY
            DATE(register_time)
        ORDER BY
            date ASC
    </select>
</mapper>