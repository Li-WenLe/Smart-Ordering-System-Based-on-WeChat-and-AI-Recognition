<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxprogrem.mapper.OrderMapper">
    <select id="search" resultType="com.wxprogrem.pojo.Order">
        SELECT * FROM `order`
        <where>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="orderId != null and orderId != ''">
                AND order_id = #{orderId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY order_time DESC
    </select>
    <update id="update" parameterType="com.wxprogrem.pojo.Order">
        update `order`
        <set>
            <if test="name != null and name!='' "> name=#{name}, </if>
            <if test="phone != null and phone!='' ">phone=#{phone}, </if>
            <if test="total != null">total=#{total}, </if>
            <if test="userId != null">user_id=#{userId}, </if>
            <if test="address != null">address=#{address}, </if>
            <if test="orderTime != null"> order_time=#{orderTime}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="updateTime != null"> update_time = #{updateTime} </if>
        </set>
        where id = #{id}
    </update>
    <select id="selectOrdersWithTimeByUserId" resultType="com.wxprogrem.dto.OrderWithTime">
        SELECT order_id as orderId, order_time as orderTime
        FROM `order`
        WHERE user_id = #{userId}
        ORDER BY order_time DESC
    </select>
</mapper>